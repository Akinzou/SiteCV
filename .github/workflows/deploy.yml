name: Deploy to VPS

on:
  push:
    branches: [master]
  workflow_dispatch:

env:
  IMAGE_NAME: sitecv
  CONTAINER_NAME: sitecv

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker build -t ${{ env.IMAGE_NAME }} .

      - name: Save Docker image
        run: |
          docker save ${{ env.IMAGE_NAME }} -o sitecv.tar

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add VPS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.VPS_PORT }} ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts 2>/dev/null

      - name: Upload image to VPS
        run: |
          scp -o StrictHostKeyChecking=no -P ${{ secrets.VPS_PORT }} sitecv.tar ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:~/

      - name: Deploy on VPS
        run: |
          ssh -o StrictHostKeyChecking=no -p ${{ secrets.VPS_PORT }} ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
            docker load -i ~/sitecv.tar
            docker stop ${{ env.CONTAINER_NAME }} 2>/dev/null || true
            docker rm ${{ env.CONTAINER_NAME }} 2>/dev/null || true
            docker run -d -p 8080:80 --name ${{ env.CONTAINER_NAME }} --restart unless-stopped ${{ env.IMAGE_NAME }}
            rm ~/sitecv.tar
            echo "Container updated successfully!"
          EOF

      - name: Cleanup
        run: rm -f sitecv.tar
